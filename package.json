{
  "name": "portfolio",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "generate:gh-pages": "GH_PAGES=true nuxt generate",
    "preview": "nuxt preview",
    "preview:dist": "serve dist",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "nuxt": "^3.17.1"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.4",
    "tailwindcss": "^4.1.4"
  }
}
